extends CPUParticles2D

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# Called when the node enters the scene tree for the first time.
func _ready():
	#restart()
	pass

#func _process(delta):
	
	#var player_frame = get_parent().frame
	#var offset = player_frame
#	texturez = get_parent().get_node("AnimationPlayer").get
	#texture.set_texture(texturez)

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
